## Agent skills

### Issue tracker

Work items are tracked as local markdown files under `local-file/tasks/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — one `CONTEXT.md` at the repo root with ADRs under `docs/adr/`. See `docs/agents/domain.md`.
